# Восстановление проекта (29.07.2026)

Рабочий чат, в котором велась вся работа по M₀, был удалён. Ниже — что уцелело, откуда взято
и что проверено.

## Источник

Единственный источник — экспорт истории чата (`чат история.md`, 111 850 байт, 1 725 строк),
предоставленный владельцем проекта. Ничего из перечисленного ниже не реконструировалось «по
смыслу»: файлы извлечены из экспорта дословно.

## Что восстановлено

| Файл | Объём | Проверка |
| --- | --- | --- |
| `witness/m0.py` | 532 строки | синтаксически валиден; прогнан, см. ниже |
| `docs/STATE_OF_PROJECT_v5.1.md` | 27 КБ | полный текст, до маркера «Конец State of Project» |

Прогон восстановленного `m0.py` (Python 3, без Lean):

```
Phase B: 2000 cases, 0 discrepancies (1978 aborts total, avg 2.6 waves/case)
Negative [wrong_order]:       232/232 caught (100.0%)
Negative [schedule_counter]:  300/300 caught (100.0%)
Negative [no_validation]:     146/300 caught (48.7%)
RESULT: ALL CHECKS PASSED
```

Цифры совпадают с зафиксированными в State of Project v5.1 (wrong order и scheduleCounter —
100%, missing validation ≈ 49.5%). Это и есть доказательство того, что восстановление
дословное, а не обрезанное.

## Найдено позже, 29.07 (тот же день)

Владелец нашёл на диске `Lemma4.lean` (две побайтово идентичные копии) и `Lemma3_5.lean`.
Оба файла лежат в `lean/`, компилируются Lean 4.31.0 без ошибок и без `sorry`, набор аксиом
совпадает с записанным в State of Project. Phase A витнесса против них: 100/100 совпадений.
Подробности и вывод `#print axioms` — в [`../lean/README.md`](../lean/README.md).

Полный прогон витнесса поверх восстановленного Lean (seed 2026): Phase A 40/40, Phase B 3000
кейсов 0 расхождений, Phase C (adversarial) 3000 кейсов 0 расхождений, negative mode
wrong_order 317/317, scheduleCounter 400/400, no_validation 198/400 (49.5%).

В экспорте чата Lean-исходников не было вообще: только сигнатура `SoundSubstrate`, имена
теорем и прозаические описания. Файлы с диска закрыли этот пробел: сначала `Lemma4.lean` и
`Lemma3_5.lean`, затем `Lemma1.lean`, `Lemma2.lean` и `Lemma3.lean` (3A + 3B с
`scheduleCounter`).

## Итог

Потеряно: ничего из содержательного. Восстановлено: пять Lean-файлов (все компилируются,
0 `sorry`), витнесс `m0.py`, State of Project v5.1. Не восстановлен только сам ход диалога —
но его роль выполняет State of Project.
